spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mariner105/spring-cloud-config-server-repository
          default-label: master #my main Git branch is master
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: springCloud_Pwd!